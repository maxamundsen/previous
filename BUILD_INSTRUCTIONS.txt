# DEVELOPMENT ENVIRONMENT

Build a debug version of the  program with the following:
go build -tags=debug

To debug the program from the CLI, run the following:
dlv debug --build-flags "-tags=debug"

To debug graphically, run:
gdlv -tags "debug" debug

# TESTING / BENCHMARKING
go test -v ./...

go test -bench=. ./...