Needs fixing:
- Return []Identity instead of []*Identity in session

Project goals:

Completed:
- Configuration file (JSON format)
- Dynamic routing with handler functions
- View Engine
    > Templating
    > ViewModel struct containing identity and viewdata
    > HTTP Error pages
    > HTMX / AJAX pages with data submission
- JSON
    > http reponses
    > 3rd party api fetching
- Session based authentication
    > get all sessions containing current user (see all logins)
    > delete all sessions containing the current user (logout all)
- Embedded files 
    > view templates for release builds
    > *Optional* embedded static assets for release builds